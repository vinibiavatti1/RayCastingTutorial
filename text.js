/*let p = {
    x: 0,
    y: 0,
    angle: 0,
    fov: 60
}
let w = 160;

let a = radiansToDegrees(Math.atan2(5 - p.y, 5- p.x) - (p.angle * Math.PI / 180));
if(a < 0 || a > p.fov) {
    console.log("NAO");
    
}
console.log(a);

let x = w * a / p.fov;
console.log(w - x);


function radiansToDegrees(radians) {
     return 180 * radians / Math.PI;
}*/

let tan = Math.tan((-1 * Math.PI / 180));
console.log(tan);